apiVersion: v1
kind: ConfigMap
metadata:
  name: {{name}}-config
  labels:
    app: {{name}}
data:
  NODE_ENV: "production"
  PORT: "3000"
  MONGO_URI: "mongodb://mongodb:27017/{{name}}"
  REDIS_URL: "redis://redis:6379"
